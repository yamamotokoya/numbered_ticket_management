<%= form_with url: admin_not_checked_in_path, method: :get, local: true do |f| %>
  <%= f.hidden_field :viewing_time_id, value: viewing_time.id %>
  <%= f.submit "受付が済んでない方の一覧", class: "btn btn-danger"  %>
<% end %>